{{#section 'head'}}
<!-- stuff that goes in head...example: -->
<link href="/CSS/member.css" rel="stylesheet">
{{/section}}


<div class="container.is-fullhd my-5">
    <div class="columns custom-flex">
        <div class="column is-one-quarter bg-555 custom-border-right">
            <p class="has-centered-text">David put stuff here</p>
        </div>
        <div class="column is-two-thirds bg-555 pt-4">
            <h1 class="is-size-2 has-centered-text">My Notes</h1>
            <div class="line"></div>

            {{#if loggedIn}}
            {{#each user.Notes as |note|}}
            <div class="custom-card-header">
                <h2 class="is-size-3">{{note.title}}</h2>
            </div>
            <div class="custom-card-body">
                <p>{{note.description}}</p>
            </div>
            {{/each}}
            {{/if}}
        </div>
    </div>
</div>


{{!-- <div class="dropdown">
    <div class="dropdown-trigger">
        <button class="button" aria-haspopup="true" aria-controls="dropdown-menu3">
            <span>Click me</span>
            <span class="icon is-small">
                <i class="fas fa-angle-down" aria-hidden="true"></i>
            </span>
        </button>
    </div>
    <div class="dropdown-menu" id="dropdown-menu3" role="menu">
        <div class="dropdown-content">
            <a href="#" class="dropdown-item">
                Songs
            </a>
            <a href="#" class="dropdown-item">
                Playlist
            </a>
            <a href="#" class="dropdown-item">
                mood
            </a>
            <a href="#" class="dropdown-item">
                notes
            </a>
        </div>
    </div>
</div> --}}